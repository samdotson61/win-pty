[project]
name = "agent-pty"
version = "0.1.0"
description = "Persistent PTY tool for LLM coding agents, tmux-backed"
requires-python = ">=3.11"
dependencies = [
    "libtmux>=0.36",
    "mcp>=1.2",
]

[project.optional-dependencies]
dev = ["pytest>=8"]

[project.scripts]
win-pty = "agent_pty.cli:main"
win-pty-mcp = "agent_pty.mcp:main"
# Upstream names kept as aliases for drop-in compatibility.
agent-pty = "agent_pty.cli:main"
agent-pty-mcp = "agent_pty.mcp:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["agent_pty"]

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "manual: tests that require external services (e.g. real claude CLI); skipped by default in CI",
]
addopts = "-m 'not manual'"
